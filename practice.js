const people = [
  { name: "alice", age: 20 },
  { name: "bob", age: 25 },
  { name: "cara", age: 22 }
];

let takeNames = people.map((name,name2,name3)  => {name.name ,
    constMakeUppercase = toUpperCase(name);
    let toArray = [takeNames ];
    name2 = people.name2
return{toArray : people.name}},

console.log(toArray)

)